<script type="text/javascript">
  // <![CDATA[    
  var so = new SWFObject("/amcolumn/amcolumn.swf", "clicks_chart", "500", "180", "8", "#FFFFFF");
  so.addVariable("path", "/amcolumn/");
  so.addVariable("settings_file", escape("<%= chart_settings_vurls_path(:format => 'xml', :period => current_period) %>"));
  so.addVariable("data_file", escape("<%= real_time_clicks_vurl_path(@vurl, :format => :xml, :period => current_period) %>"));
  so.addVariable("preloader_color", "#000000");
  so.write("clicks_chart");
  // ]]>
</script>
